# Application Environment
NODE_ENV=development

# Database Configuration
# SQLite database file path
DATABASE_URL=drizzle/db/sqlite.db
DATABASE_TYPE=sqlite
# Enable query logging for debugging
DATABASE_LOG_QUERIES=false

# Application Server Configuration
APP_PORT=3000
APP_HOST=localhost

# Better Auth Configuration (REQUIRED)
# Secret key for signing tokens - generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-replace-in-production
BETTER_AUTH_URL=http://localhost:5173
VITE_BETTER_AUTH_URL=http://localhost:5173

# For production, consider using PostgreSQL:
# DATABASE_TYPE=postgres
# DATABASE_URL=postgresql://user:password@localhost:5432/budget_db

# AI Features (Optional)
# Get your API key from https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# LLM API Key Encryption (REQUIRED for production)
# These keys are used to encrypt API keys stored in the database
# Generate a secure random key: openssl rand -base64 32
# LLM_ENCRYPTION_KEY=your-32-character-secret-key
# LLM_ENCRYPTION_SALT=your-unique-salt-value

# Bank Connection Providers (Optional)
# These enable automatic transaction syncing from bank accounts

# Teller (https://teller.io)
# Sign up for free developer account (100 live connections)
# TELLER_APPLICATION_ID=app_...
# TELLER_ENVIRONMENT=sandbox
# VITE_TELLER_APPLICATION_ID=app_...
# VITE_TELLER_ENVIRONMENT=sandbox

# SimpleFIN (https://simplefin.org)
# No server config needed - users provide their own Access URL ($15/year)

# Bank Connection Credential Encryption
# Uses ENCRYPTION_SECRET if set, otherwise falls back to BETTER_AUTH_SECRET
# For production, set a separate encryption key:
# ENCRYPTION_SECRET=your-separate-encryption-key